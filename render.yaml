services:
  - type: web
    name: whatsapp-baileys
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: BAILEYS_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20.11.0
